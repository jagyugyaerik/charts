apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-20.fullname" . }}
  labels:
    {{- include "nginx-20.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginx-20.selectorLabels" . | nindent 4 }}
